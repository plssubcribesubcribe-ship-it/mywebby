<svg xmlns="http://www.w3.org/2000/svg" width="600" height="400" viewBox="0 0 600 400">
  <foreignObject width="100%" height="100%">
    <div xmlns="http://www.w3.org/1999/xhtml" style="width:600px;height:400px;overflow:auto;">
<!DOCTYPE html>
<html>
<head>
<meta charset="UTF-8">
<title>Paid Access</title>
<style>
body{
    font-family:Arial,sans-serif;
    background:#111;
    color:white;
    text-align:center;
    padding:40px;
}
input,button{
    padding:10px;
    margin:5px;
}
#adminPanel{
    display:none;
    border:1px solid #444;
    padding:20px;
    margin-top:20px;
}
</style>
</head>
<body>

<h1>Paid Access</h1>

<input id="codeInput" placeholder="Access Code"/>
<button onclick="login()">Enter</button>

<div id="terms" style="display:none;">
    <h3>Terms & Conditions</h3>
    <p>No sharing. No refunds.</p>
    <button onclick="openCherry()">CHERRY</button>
    <button onclick="openNocturne()">nocturne</button>
    <button onclick="openGnMath()">GnMath</button>
</div>

<div id="adminPanel">
    <h2>Admin Panel</h2>

    <input id="newCode" placeholder="New Code"/>
    <button onclick="addCode()">Add Code</button>

    <h3>Codes</h3>
    <ul id="codeList"></ul>

    <h3>Logs</h3>
    <ul id="logList"></ul>
</div>

<script type="module">

import { initializeApp } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-app.js";
import { getDatabase, ref, get, set, remove, push } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-database.js";
import { getAuth, signInAnonymously } from "https://www.gstatic.com/firebasejs/10.12.2/firebase-auth.js";

const firebaseConfig = {
  apiKey: "AIzaSyDItm8wa73Vc-27G41YFMsoTbpJKE1eqyI",
  authDomain: "dontleak-b54c1.firebaseapp.com",
  databaseURL: "https://dontleak-b54c1-default-rtdb.firebaseio.com",
  projectId: "dontleak-b54c1",
  storageBucket: "dontleak-b54c1.firebasestorage.app",
  messagingSenderId: "87824315321",
  appId: "1:87824315321:web:6e8e145cabfc56a4c3fc68"
};

const app = initializeApp(firebaseConfig);
const db = getDatabase(app);
const auth = getAuth(app);

let uid = "unknown";

signInAnonymously(auth).then((cred)=>{
    uid = cred.user.uid;
});

window.login = async function() {

    const code = document.getElementById("codeInput").value;

    if(code === "FRFRNOLEAK"){
        document.getElementById("adminPanel").style.display = "block";
        loadCodes();
        loadLogs();
        return;
    }

    const snap = await get(ref(db,"codes/"+code));

    if(snap.exists()){
        await push(ref(db,"logs"),{
            code:code,
            uid:uid,
            time:new Date().toLocaleString()
        });

        document.getElementById("terms").style.display = "block";
    } else {
        alert("Invalid code");
    }
};

window.openCherry = function(){

    const win = window.open("about:blank","_blank");

    win.document.write(`
    <!DOCTYPE html>
    <html>
    <body style="margin:0;">
        <iframe
        src="https://cdn.jsdelivr.net/gh/plssubcribesubcribe-ship-it/CHERRY@main/index.svg"
        style="width:100vw;height:100vh;border:none;">
        </iframe>
    </body>
    </html>
    `);

    win.document.close();
};

window.openNocturne = function(){

    const win = window.open("about:blank","_blank");

    win.document.write(`
    <!DOCTYPE html>
    <html>
    <body style="margin:0;">
        <iframe
        src="https://cdn.jsdelivr.net/gh/plssubcribesubcribe-ship-it/mywebby@main/index.svg"
        style="width:100vw;height:100vh;border:none;">
        </iframe>
    </body>
    </html>
    `);

    win.document.close();
};

window.openGnMath = function(){

    const win = window.open("about:blank","_blank");

    win.document.write(`
    <!DOCTYPE html>
    <html>
    <body style="margin:0;">
        <iframe
        src="https://cdn.jsdelivr.net/gh/plssubcribesubcribe-ship-it/mywebby@main/gnmath.svg"
        style="width:100vw;height:100vh;border:none;">
        </iframe>
    </body>
    </html>
    `);

    win.document.close();
};

window.addCode = async function(){

    const code = document.getElementById("newCode").value;

    if(!code) return;

    await set(ref(db,"codes/"+code),true);

    loadCodes();
};

async function loadCodes(){

    const list = document.getElementById("codeList");
    list.innerHTML = "";

    const snap = await get(ref(db,"codes"));

    if(!snap.exists()) return;

    const data = snap.val();

    for(const code in data){

        const li = document.createElement("li");

        li.innerHTML = code + ` <button onclick="deleteCode('${code}')">Delete</button>`;

        list.appendChild(li);
    }
}

window.deleteCode = async function(code){

    await remove(ref(db,"codes/"+code));

    loadCodes();
};

async function loadLogs(){

    const list = document.getElementById("logList");
    list.innerHTML = "";

    const snap = await get(ref(db,"logs"));

    if(!snap.exists()) return;

    const data = snap.val();

    for(const id in data){

        const item = data[id];

        const li = document.createElement("li");

        li.textContent =
            item.time +
            " | " +
            item.code +
            " | UID: " +
            item.uid;

        list.appendChild(li);
    }
}

</script>

</body>
</html>
    </div>
  </foreignObject>
</svg>